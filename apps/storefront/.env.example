# Medusa Backend
MEDUSA_BACKEND_URL=http://localhost:9000
MEDUSA_PUBLISHABLE_KEY=

# Public Store URL (used for sitemap, SEO, Stripe return URLs)
PUBLIC_STORE_URL=http://localhost:5173

# Database (auth sessions)
DATABASE_URL=postgresql://localhost:5432/ec1

# Meilisearch
MEILISEARCH_HOST=http://localhost:7700
MEILISEARCH_API_KEY=

# Stripe
PUBLIC_STRIPE_PUBLISHABLE_KEY=

# WebAuthn (passkey auth)
WEBAUTHN_RP_ID=localhost
WEBAUTHN_RP_NAME=EC1
WEBAUTHN_ORIGIN=http://localhost:5173

# Redis (shared rate limiting + WebAuthn challenge store; optional for local dev)
REDIS_URL=redis://localhost:6379

# Email (Resend — optional, logs emails when not set)
RESEND_API_KEY=
EMAIL_FROM=EC1 Store <noreply@sellio.fi>
CONTACT_EMAIL=hello@sellio.fi

# Cache invalidation (shared secret with backend)
CACHE_SECRET=

# Sentry (error tracking)
PUBLIC_SENTRY_DSN=
